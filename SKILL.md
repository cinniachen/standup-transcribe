---
name: standup-transcribe
description: 把中文脱口秀节目、开放麦录像、国外standup、访谈/播客里的喜剧片段，转成可复盘、可收藏、可二次学习的喜剧技巧笔记。支持视频/音频转录、bit块拆分、五维喜剧方法论标注、可模仿技巧提炼、创作练习题、本地Word/Markdown产出，以及可选上传到金山文档、飞书等在线文档。用户说脱口秀转录、喜剧技巧分析、包袱拆解、开放麦复盘、standup学习笔记、把视频变成学习文档、上传金山文档时使用。
metadata:
  emoji: "🎤"
  requires:
    bins: ["ffmpeg"]
---

# 脱口秀学习笔记生成器

目标：把一个喜剧视频/音频变成一份能反复学习的文档，而不是只得到一份逐字稿。

核心产物：

1. 完整转录稿
2. 按bit/话题块整理的重点段落
3. 喜剧技巧标签
4. 为什么好笑
5. 可学习点
6. 可迁移写法
7. 金句收藏
8. 整场结构图
9. 表演者风格总结
10. 创作练习题
11. 可选：上传到金山文档、飞书或其他在线文档

## 使用原则

- 默认做"学习笔记"，不是商业发布稿。
- 30分钟以内素材最适合完整处理。超过30分钟时，先提醒用户建议拆分；如果用户坚持，可以只做完整转录，标注部分精选高价值bit。
- 标注粒度以bit/话题块为主，不逐句机械标注。
- 时间戳放在bit块开头，方便回看，不需要每句话都有。
- 上传云文档前，提醒用户：转录稿和视频内容可能涉及隐私或版权，确认后再上传。
- 如果用户没有指定发布平台，默认只生成本地文件。

## 快速流程

当用户给出视频、音频或文本时：

1. 确认输入类型：视频/音频/已有文本。
2. 询问或推断学习目标：看懂包袱、整理可模仿技巧、反推创作练习，默认三者都做。
3. 询问演出信息：表演者、节目/场次、主要嘉宾、人名词表；如果用户不知道，先处理，转录后复核可疑人名。
4. 转录并校对：使用 `scripts/transcribe.py`。
5. 拆分bit块：按话题、观点、场景转换、连续包袱组来切分。
6. 做五维方法论分析：需要时读取 `references/annotation-framework.md`。
7. 生成学习笔记：按 `references/output-template.md` 的结构输出。
8. 生成练习题：仿写、改写、结构复盘、个人素材迁移。
9. 生成本地文件：优先 Word，也可 Markdown/JSON。
10. 如用户确认，调用对应发布方式上传在线文档；金山文档方案见 `references/publishing.md`。

## 处理模式

### 1. 完整学习笔记模式

用户说"帮我学习这个视频""做喜剧技巧分析""把这个变成学习文档"时使用。

产出：

- `完整转录稿.docx`
- `喜剧技巧学习笔记.docx`
- 可选 `segments.json`
- 可选在线文档链接

成功标志：

- 文档里有完整转录稿。
- 重点分析按bit块呈现。
- 每个重点bit至少包含：原文摘要、技巧标签、为什么好笑、可学习点、可迁移写法。
- 文末有金句收藏、结构图、风格总结、练习题。

### 2. 快速转录模式

用户只说"转录一下""视频转字幕"，且没有要求学习分析时使用。

产出：

- 带时间戳转录稿
- 纯文本稿

完成后主动问一句：是否继续生成喜剧技巧学习笔记。

### 3. 已有文本分析模式

用户直接贴文本或给已有稿件时，不跑转录，直接拆bit并分析。

如果文本明显缺少上下文，先问：

- 这是完整段子还是片段？
- 表演者是谁？
- 你更想学习结构、包袱写法，还是改写成自己的素材？

### 4. 批量学习包模式

用户给多个视频时：

- 每个视频单独生成一份学习笔记。
- 额外生成一份合集：共同技巧、表演者差异、可迁移模板、练习题合集。
- 支持单独调用每个视频结果。

## 转录调用

优先使用当前skill的脚本：

```bash
python3 scripts/transcribe.py 视频文件.mp4
python3 scripts/transcribe.py -o 输出目录 视频文件.mp4
python3 scripts/transcribe.py 视频1.mp4 视频2.mp4
```

如果作为Python模块调用：

```python
from transcribe import process_single, process_batch

result = process_single(
    video_path="/path/to/video.mp4",
    output_dir="/path/to/output",
    title="表演者 - 场次",
    corrections={"易错名": "正确名"},
    model="base"
)
```

## 人名和关键词校对

人名是高风险点。即使有 `names.json`，也要主动做二次复核。

处理规则：

1. 转录前尽量问用户：表演者、节目名、嘉宾、主持人、常出现的人名。
2. 转录后列出"可疑人名/词语"让用户确认，尤其是重复出现但不在 `names.json` 的2-4字中文词。
3. 用户确认后，把修正映射传入 `corrections`。
4. 如果用户明确说"以后都这样改"，提醒可以更新 `scripts/names.json`。

可疑人名判断：

- 同一个陌生词反复出现。
- 同音近形的人名，如"鸟鸟/妙妙/牛牛"。
- 主持人、嘉宾、节目名被转成普通词。
- 国外演员名被音译成多个版本。

## 分析要求

核心方法论保留"五维框架"，但最终文档要面向学习者，不要只堆术语。

每个重点bit推荐写成：

- 时间戳
- bit名称
- 原文/摘要
- 五维观察
- 技巧标签
- 为什么好笑
- 我能怎么学
- 可迁移写法
- 可选：仿写练习

读取：

- 五维方法论：`references/annotation-framework.md`
- 输出模板：`references/output-template.md`
- 排查步骤：`references/troubleshooting.md`
- 发布平台：`references/publishing.md`

## 云文档发布

只有用户明确要求上传时才上传。

上传前先说：

"这个文档会包含视频转录和学习分析，可能涉及私人素材或版权内容。确认要上传到云文档吗？"

默认发布顺序：

1. 先生成本地Word/Markdown作为备份。
2. 再上传云文档。
3. 返回在线链接和本地备份路径。

金山文档不是核心能力，而是发布层。不要让上传失败阻断本地学习笔记生成。

## 排查入口

遇到失败时，先根据现象读取 `references/troubleshooting.md`。

常见问题：

- 找不到ffmpeg
- 没安装whisper
- 视频路径错误
- 文件太长或太大
- 人名错误
- Word已生成但云文档上传失败
- 生成内容太长、不适合学习
